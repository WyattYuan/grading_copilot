学生姓名: 张三
学号: 1001
性别: 男

[作答: q1]
TDD 就是测试驱动开发,核心流程是先写测试,然后写代码通过测试,最后再重构优化代码。

[作答: q2]
def fibonacci(n):
    if n == 0:
        return 0
    if n == 1:
        return 1
    return fibonacci(n-1) + fibonacci(n-2)

[作答: q3]
依赖注入就是把对象需要的依赖从外部传进来,而不是在对象内部自己创建。这样做的好处是降低了代码的耦合度,让代码更容易测试和维护。
