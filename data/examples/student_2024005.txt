学生姓名: 孙待提高
学号: 2024005
性别: 男

[作答: q1]
列表和元组：
列表就是用来存数据的，元组也是。

列表写法是[1,2,3]这样的，元组是(1,2,3)。

不太知道什么时候用哪个，感觉都差不多吧。

[作答: q2]
```python
def sum_of_even_squares(nums):
    for i in nums:
        if i % 2:
            sum = i * i
    return sum
```

这个函数算偶数的平方和。

[作答: q3]
装饰器是一种Python的语法。

它用@开头，写在函数前面。

可以用来做一些特殊的事情，比如...不太记得了，好像是可以改变函数的行为。

[作答: q4]
```python
def fibonacci(n):
    if n == 0:
        return 0
    if n == 1:
        return 1
    return fibonacci(n) + fibonacci(n)
```

斐波那契数列就是1,1,2,3,5这样的，每个数是前面两个数的和。用递归写的话就是这样。
